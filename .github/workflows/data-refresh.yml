name: Data refresh

on:
  schedule:
    # Weekly current-season merge refresh (Monday 16:00 UTC)
    - cron: '0 16 * * 1'
    # Monthly full rebuild (1st of month 16:00 UTC)
    - cron: '0 16 1 * *'
  workflow_dispatch:
    inputs:
      mode:
        description: Refresh mode
        required: true
        type: choice
        options:
          - current
          - full
        default: current
      skip_link_enrichment:
        description: Skip crawling team websites for links (recommended on schedule)
        required: true
        type: boolean
        default: true
      open_pr:
        description: Open a PR when the seed changes (ignored when DATA_REFRESH_OPEN_PR=false)
        required: true
        type: boolean
        default: true

permissions:
  contents: write
  pull-requests: write

concurrency:
  group: data-refresh
  cancel-in-progress: false

jobs:
  refresh:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version-file: .nvmrc
          cache: npm

      - run: npm ci

      - name: Resolve refresh options
        id: opts
        shell: bash
        run: |
          set -euo pipefail
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            MODE="${{ inputs.mode }}"
            SKIP="${{ inputs.skip_link_enrichment }}"
            OPEN_PR_INPUT="${{ inputs.open_pr }}"
          elif [ "${{ github.event.schedule }}" = "0 16 * * 1" ]; then
            MODE="current"
            SKIP="true"
            OPEN_PR_INPUT="true"
          else
            MODE="full"
            SKIP="true"
            OPEN_PR_INPUT="true"
          fi

          # Repo variable DATA_REFRESH_OPEN_PR defaults to on; set to "false" to disable.
          if [ "${{ vars.DATA_REFRESH_OPEN_PR }}" = "false" ] || [ "${OPEN_PR_INPUT}" != "true" ]; then
            OPEN_PR="false"
          else
            OPEN_PR="true"
          fi

          echo "mode=${MODE}" >> "$GITHUB_OUTPUT"
          echo "skip_link_enrichment=${SKIP}" >> "$GITHUB_OUTPUT"
          echo "open_pr=${OPEN_PR}" >> "$GITHUB_OUTPUT"
          echo "Resolved mode=${MODE} skip_link_enrichment=${SKIP} open_pr=${OPEN_PR}"

      - name: Pull public FTC data
        run: |
          set -euo pipefail
          ARGS=(--mode="${{ steps.opts.outputs.mode }}")
          if [ "${{ steps.opts.outputs.skip_link_enrichment }}" = "true" ]; then
            ARGS+=(--skip-link-enrichment)
          fi
          npm run pull:data -- "${ARGS[@]}"

      - name: Validate seed, observations, and snapshot tree
        run: npm run validate:data

      - name: Upload refresh artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: data-refresh-${{ steps.opts.outputs.mode }}-${{ github.run_id }}
          path: |
            data-refresh-report.md
            snapshot-tree-report.md
            src/data/nv-ftc-teams.generated.json
            src/data/nv-ftc-team-observations.generated.json
            public/data/manifest.json
            public/data/source-health.json
            public/data/regions/**
          if-no-files-found: warn

      - name: Detect snapshot changes
        id: changes
        shell: bash
        run: |
          set -euo pipefail
          SEED_PATH="src/data/nv-ftc-teams.generated.json"
          OBS_PATH="src/data/nv-ftc-team-observations.generated.json"
          CHANGED=false

          if ! git diff --quiet -- "$SEED_PATH"; then
            CHANGED=true
            echo "Mega-seed changed."
            git diff --stat -- "$SEED_PATH"
          fi

          if [ -f "$OBS_PATH" ] && ! git diff --quiet -- "$OBS_PATH"; then
            CHANGED=true
            echo "Observations side store changed."
            git diff --stat -- "$OBS_PATH"
          fi

          if [ "$CHANGED" = "true" ]; then
            echo "changed=true" >> "$GITHUB_OUTPUT"
            echo "Checked-in snapshot inputs changed (tree will regenerate on next build)."
          else
            echo "changed=false" >> "$GITHUB_OUTPUT"
            echo "No seed/observations changes."
          fi

      - name: Prepare pull request body
        if: steps.changes.outputs.changed == 'true' && steps.opts.outputs.open_pr == 'true'
        shell: bash
        run: |
          set -euo pipefail
          {
            echo "Automated data refresh (\`mode=${{ steps.opts.outputs.mode }}\`)."
            echo
            echo "## Snapshot pipeline (#90)"
            echo
            echo "- \`pull:data\` refreshed the mega-seed (+ observations) and emitted the #87 snapshot tree under \`public/data/\`."
            echo "- \`validate:data\` re-validated the seed, observations, manifest, and tree files (publish guard still blocks empty/drop publishes)."
            echo "- This PR commits **checked-in** inputs only (\`nv-ftc-teams.generated.json\` + \`nv-ftc-team-observations.generated.json\`)."
            echo "- The split tree remains **gitignored**; Pages/Worker \`npm run build\` (\`sync:data\`) regenerates it for \`/data/*\`."
            echo "- Download the workflow artifact for \`manifest.json\`, \`source-health.json\`, region summaries, and \`snapshot-tree-report.md\`."
            echo
            echo "Publish gates: \`publishGuard\` + \`npm run validate:data\`."
            echo "This workflow never pushes directly to \`main\` and does not auto-merge."
            echo
            if [ -f data-refresh-report.md ]; then
              cat data-refresh-report.md
            else
              echo "_No change report file produced._"
            fi
            echo
            if [ -f snapshot-tree-report.md ]; then
              cat snapshot-tree-report.md
            else
              echo "_No snapshot-tree report produced._"
            fi
          } > "$RUNNER_TEMP/data-refresh-pr-body.md"

      - name: Create pull request
        if: steps.changes.outputs.changed == 'true' && steps.opts.outputs.open_pr == 'true'
        uses: peter-evans/create-pull-request@v7
        with:
          commit-message: "chore(data): refresh Nevada FTC snapshot (#3)"
          title: "chore(data): refresh Nevada FTC snapshot"
          body-path: ${{ runner.temp }}/data-refresh-pr-body.md
          branch: data-refresh/${{ steps.opts.outputs.mode }}
          delete-branch: true
          add-paths: |
            src/data/nv-ftc-teams.generated.json
            src/data/nv-ftc-team-observations.generated.json
          labels: |
            type: data
            data-quality
